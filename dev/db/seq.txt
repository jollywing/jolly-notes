#创建表格
CREATE TABLE SPELL ( ID SMALLINT NOT NULL, NAME VARCHAR(10), MAGIC SMALLINT , DAMAGE SMALLINT)       

#向表格中插入数据
方法1：INSERT INTO SPELL VALUES(1, 'mana burn', 10, 50)
方法2：INSERT INTO PERS (ID, NAME, DEPT, JOB, YEARS, SALARY) SELECT ID, NAME, DEPT, JOB, YEARS, SALARY FROM STAFF WHERE DEPT = 38
方法3：INSERT INTO PERS (NAME, JOB, ID) VALUES ('Swagerman', 'Prgmr', 500), ('Limoges', 'Prgmr', 510), ('Li', 'Prgmr', 520)

#显示表格的内容
SELECT * FROM SPELL

#更新表格
UPDATE SPELL SET MAGIC = 15 WHERE NAME = 'mana burn'

#删除表格中的一行
DELETE FROM SPELL WHERE ID = 120

#删除整个表格
DROP TABLE SPELL

#生成视图
CREATE VIEW STAFF_ONLY AS SELECT ID, NAME, DEPT, JOB, YEARS FROM STAFF WHERE JOB <> 'Mgr' AND DEPT=20
SELECT * FROM STAFF_ONLY

